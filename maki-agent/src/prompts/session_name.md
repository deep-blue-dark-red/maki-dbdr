Generate a concise, descriptive session name based on the first user interaction. The name should capture the primary topic or goal.

Rules:
- 4–6 words, kebab-case (e.g. `debug-auth-firewall`, `refactor-payment-flow`)
- No punctuation other than hyphens
- Return ONLY the session name — no explanation, no quotes, nothing else
