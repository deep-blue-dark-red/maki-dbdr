Generate a concise, descriptive session name based on the first few user messages from a conversation. The name should capture the primary topic or goal.

Rules:
- 2–5 words, kebab-case (e.g. `debug-auth-middleware`, `refactor-payment-flow`)
- No punctuation other than hyphens
- Return ONLY the session name — no explanation, no quotes, nothing else
